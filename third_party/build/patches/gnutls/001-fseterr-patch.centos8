diff -Naur gnutls-3.1.23/gl/fseterr.c gnutls-3.1.23.patched/gl/fseterr.c
--- gnutls-3.1.23/gl/fseterr.c	2013-04-02 20:27:32.000000000 +0000
+++ gnutls-3.1.23.patched/gl/fseterr.c	2020-08-25 20:25:13.799593815 +0000
@@ -29,7 +29,7 @@
   /* Most systems provide FILE as a struct and the necessary bitmask in
      <stdio.h>, because they need it for implementing getc() and putc() as
      fast macros.  */
-#if defined _IO_ftrylockfile || __GNU_LIBRARY__ == 1 /* GNU libc, BeOS, Haiku, Linux libc5 */
+#if defined _IO_ftrylockfile || __GNU_LIBRARY__ >= 1 /* GNU libc, BeOS, Haiku, Linux libc5 */
   fp->_flags |= _IO_ERR_SEEN;
 #elif defined __sferror || defined __DragonFly__ /* FreeBSD, NetBSD, OpenBSD, DragonFly, Mac OS X, Cygwin */
   fp_->_flags |= __SERR;
