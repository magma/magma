# Magma sysctl parameters

# core dump pattern: see core(5) for info
kernel.core_pattern = |/usr/local/bin/coredump core-%t-%e-%p

# disable sysrq: for info see
# https://www.kernel.org/doc/html/latest/admin-guide/sysrq.html
kernel.sysrq = 0
