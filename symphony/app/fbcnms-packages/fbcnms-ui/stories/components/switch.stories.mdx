import Checkbox from '../../components/design-system/Checkbox/Checkbox';
import Switch from '../../components/design-system/switch/Switch';
import Text from '../../components/design-system/Text';
import {Meta, Preview, Story} from '@storybook/addon-docs/blocks';
import {STORY_CATEGORIES} from '../storybookUtils';
import {makeStyles} from '@material-ui/styles';
import {useState} from 'react';

export const useStyles = makeStyles(_theme => ({
  sample: {
    display: 'flex',
    margin: '8px',
    '& > *': {
      marginRight: '4px',
    },
  },
  optionsContainer: {
    display: 'flex',
    flexDirection: 'column',
    marginTop: '32px',
  },
  optionCheckbox: {
    marginTop: '4px',
  },
}));

<Meta title={`${STORY_CATEGORIES.COMPONENTS}/Switch`} />

# Switch

```js
import Switch from '@fbcnms/ui/components/design-system/Switch';
```

`Switch` supports the `regular` and `critical` skins, as well as the `subtitle2` and `body2` variants.

<Preview>
  <Story name="Regular">
    {() => {
      const classes = useStyles();
      const [checked, setChecked] = useState(true);
      const [isDisabled, setIsDisabled] = useState(false);
      const [isBold, setIsBold] = useState(false);
      return (
        <div className={classes.root}>
          <div className={classes.sample}>
            <Switch
              checked={checked}
              title={checked ? 'On' : 'Off'}
              variant={isBold ? 'subtitle2' : 'body2'}
              onChange={setChecked}
              disabled={isDisabled}
            />
          </div>
          <div className={classes.optionsContainer}>
            <Text variant="h6">Variants:</Text>
            <Checkbox
              className={classes.optionCheckbox}
              checked={isDisabled}
              title="Show disabled"
              onChange={selection =>
                setIsDisabled(selection === 'checked' ? true : false)
              }
            />
            <Checkbox
              className={classes.optionCheckbox}
              checked={isBold}
              title="Show bold"
              onChange={selection =>
                setIsBold(selection === 'checked' ? true : false)
              }
            />
          </div>
        </div>
      );
    }}
  </Story>
</Preview>

### Critical

<Preview>
  <Story name="Critical">
    {() => {
      const [checked, setChecked] = useState(false);
      return (
        <Switch
          checked={checked}
          title={checked ? 'On' : 'Off'}
          onChange={setChecked}
          skin="critical"
        />
      );
    }}
  </Story>
</Preview>
