query LocationEquipmentsQuery($id: ID!) {
  location: node(id: $id) {
    ... on Location {
      equipments {
        id
        name
        equipmentType {
          name
        }
      }
    }
  }
}
