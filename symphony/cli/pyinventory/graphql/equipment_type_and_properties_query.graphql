query EquipmentTypeAndPropertiesQuery($id: ID!) {
  equipment(id: $id) {
    equipmentType {
      name
    }
    properties {
      propertyType {
        id
      }
      stringValue
      intValue
      booleanValue
      floatValue
      latitudeValue
      longitudeValue
    }
  }
}
