#!/usr/bin/env python3
# @generated AUTOGENERATED file. Do not Change!

from dataclasses import dataclass
from datetime import datetime
from gql.gql.datetime_utils import DATETIME_FIELD
from gql.gql.graphql_client import GraphqlClient
from gql.gql.client import OperationException
from gql.gql.reporter import FailedOperationException
from functools import partial
from numbers import Number
from typing import Any, Callable, List, Mapping, Optional
from time import perf_counter
from dataclasses_json import DataClassJsonMixin

from gql.gql.enum_utils import enum_field
from .user_role_enum import UserRole
from .user_status_enum import UserStatus


QUERY: List[str] = ["""
query UsersQuery {
  users {
    edges {
      node {
        id
        authID
        email
        status
        role
      }
    }
  }
}

"""]

@dataclass
class UsersQuery(DataClassJsonMixin):
    @dataclass
    class UsersQueryData(DataClassJsonMixin):
        @dataclass
        class UserConnection(DataClassJsonMixin):
            @dataclass
            class UserEdge(DataClassJsonMixin):
                @dataclass
                class User(DataClassJsonMixin):
                    id: str
                    authID: str
                    email: str
                    status: UserStatus = enum_field(UserStatus)
                    role: UserRole = enum_field(UserRole)

                node: Optional[User]

            edges: List[UserEdge]

        users: Optional[UserConnection]

    data: UsersQueryData

    @classmethod
    # fmt: off
    def execute(cls, client: GraphqlClient) -> Optional[UsersQueryData.UserConnection]:
        # fmt: off
        variables = {}
        try:
            start_time = perf_counter()
            response_text = client.call(''.join(set(QUERY)), variables=variables)
            res = cls.from_json(response_text).data
            elapsed_time = perf_counter() - start_time
            client.reporter.log_successful_operation("UsersQuery", variables, elapsed_time)
            return res.users
        except OperationException as e:
            raise FailedOperationException(
                client.reporter,
                e.err_msg,
                e.err_id,
                "UsersQuery",
                variables,
            )
