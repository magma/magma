query ServiceDetailsQuery($id: ID!) {
  service(id: $id) {
    id
    name
    externalId
    customer {
      id
      name
      externalId
    }
    terminationPoints {
      id
      name
    }
    links {
      id
    }
  }
}
