query GetLocationsQuery {
  locations {
    edges {
      node {
        id
        name
        latitude
        longitude
        externalId
        locationType {
          name
        }
      }
    }
  }
}
