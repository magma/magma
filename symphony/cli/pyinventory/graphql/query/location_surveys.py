#!/usr/bin/env python3
# @generated AUTOGENERATED file. Do not Change!

from dataclasses import dataclass
from datetime import datetime
from gql.gql.datetime_utils import DATETIME_FIELD
from gql.gql.graphql_client import GraphqlClient
from gql.gql.client import OperationException
from gql.gql.reporter import FailedOperationException
from functools import partial
from numbers import Number
from typing import Any, Callable, List, Mapping, Optional
from time import perf_counter
from dataclasses_json import DataClassJsonMixin

from ..fragment.survey import SurveyFragment, QUERY as SurveyFragmentQuery

QUERY: List[str] = SurveyFragmentQuery + ["""
query LocationSurveysQuery($id: ID!) {
  location: node(id: $id) {
    ... on Location {
      surveys {
        ...SurveyFragment
      }
    }
  }
}

"""]

@dataclass
class LocationSurveysQuery(DataClassJsonMixin):
    @dataclass
    class LocationSurveysQueryData(DataClassJsonMixin):
        @dataclass
        class Node(DataClassJsonMixin):
            @dataclass
            class Survey(SurveyFragment):
                pass

            surveys: List[Survey]

        location: Optional[Node]

    data: LocationSurveysQueryData

    @classmethod
    # fmt: off
    def execute(cls, client: GraphqlClient, id: str) -> Optional[LocationSurveysQueryData.Node]:
        # fmt: off
        variables = {"id": id}
        try:
            start_time = perf_counter()
            response_text = client.call(''.join(set(QUERY)), variables=variables)
            res = cls.from_json(response_text).data
            elapsed_time = perf_counter() - start_time
            client.reporter.log_successful_operation("LocationSurveysQuery", variables, elapsed_time)
            return res.location
        except OperationException as e:
            raise FailedOperationException(
                client.reporter,
                e.err_msg,
                e.err_id,
                "LocationSurveysQuery",
                variables,
            )
