# check if we have all the secrets necessary in the seed directory
- hosts: localhost
  roles:
    - services
    - services/docker
    - services/helm
    - services/orc8r
  vars_files:
    - roles/services/vars/all.yml
