---
swagger: '2.0'

info:
  title: Test
  description: Test API
  version: 1.0.0

basePath: /foo

consumes:
  - application/json

produces:
  - application/json

schemes:
  - https
  - http

tags:
  - name: AppleTag
    description: Tag apple
  - name: BananaTag
    description: Tag banana

paths:
  /barpath:
    post:
      summary: Bar
      responses:
        '200':
          description: Soap sop
          schema:
            type: string

definitions:
  foo_def:
    type: integer
