load("@com_github_bazelbuild_buildtools//buildifier:def.bzl", "buildifier")

buildifier(
    name = "buildifier",
)

# EXPERIMENTAL TEST THING： DELETE LATER
load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "hello_test",
    size = "small",
    srcs = ["hello_test.cpp"],
    deps = [
      "@com_google_googletest//:gtest_main",
      "//orc8r/protos:common_cc_proto",
    ],
)
